// import Redis from 'ioredis';

// import { ChainId } from '@infinityxyz/lib/types/core';

// import { ProcessOptions } from '@/lib/process/types';

// import { AbstractBlockProcessor } from '../block-processor/block-processor.abstract';
// import { AbstractEvent } from '../event.abstract';

// export class CumulativeMerkleDistributor extends AbstractBlockProcessor {
//   protected events: AbstractEvent<unknown>[];

//   constructor(_db: Redis, chainId: ChainId, _address: string, options?: ProcessOptions) {
//     super(_db, chainId, `cumulative-merkle-distributor:${_address}`, _address, options);
//   }
// }
