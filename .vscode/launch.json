{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "node-terminal",
        "name": "Run Script: search for matches",
        "request": "launch",
        "command": "npm run scan-matches",
        "cwd": "${workspaceFolder}"
      }
    ]
  }
  