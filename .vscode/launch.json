{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "node-terminal",
        "name": "Run Script: search for matches",
        "request": "launch",
        "command": "npm run scan-matches",
        "cwd": "${workspaceFolder}"
      },
      {
        "type": "node-terminal",
        "name": "Run Script: aggregate stats",
        "request": "launch",
        "command": "npm run aggregate",
        "cwd": "${workspaceFolder}"
      }
    ]
  }
  